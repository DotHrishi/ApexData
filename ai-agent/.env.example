# =============================================================
# ApexData ai-agent — Environment Variables
# =============================================================
# Copy this file to .env for local development
# Set these in Render / Railway environment variables for production

# Comma-separated list of allowed CORS origins in production
# Example: https://apexdata.vercel.app
# Leave unset or set to * to allow all origins (development only)
ALLOWED_ORIGINS=
